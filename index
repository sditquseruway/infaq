<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="theme-color" content="#17633b">
  <meta name="description" content="Cek riwayat pembayaran dan tunggakan infaq santri Yayasan Huda Wan Nurul Qurani.">
  <meta property="og:title" content="Laporan Infaq Santri — Yayasan Huda Wan Nurul Qurani">
  <meta property="og:description" content="Halaman khusus orang tua untuk memeriksa pembayaran dan tunggakan infaq santri.">
  <meta property="og:type" content="website">
  <meta name="twitter:card" content="summary">
  <title>Laporan Infaq Santri — Yayasan Huda Wan Nurul Qurani</title>
  <link rel="preconnect" href="https://i.ibb.co.com">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/PapaParse/5.4.1/papaparse.min.js"></script>
  <style>
    :root {
      --green-950:#0b3522; --green-900:#104a2d; --green-800:#17633b; --green-700:#1c7445;
      --green-100:#dff3e7; --green-50:#f1faf4; --gold:#e8b72f; --gold-light:#fff3c4;
      --ink:#17241d; --muted:#617068; --line:#dbe6df; --surface:#ffffff; --danger:#b42318;
      --danger-soft:#fff0ee; --success:#16794a; --shadow:0 18px 48px rgba(10,58,34,.11);
    }
    * { box-sizing:border-box; }
    body { margin:0; min-height:100vh; color:var(--ink); background:var(--green-50); font-family:Arial,Helvetica,sans-serif; letter-spacing:0; }
    button,input,a { font:inherit; letter-spacing:0; }
    button,a { -webkit-tap-highlight-color:transparent; }
    .topbar { height:7px; background:var(--gold); }
    .site-header { position:relative; overflow:hidden; color:#fff; background:var(--green-900); }
    .site-header::after { content:""; position:absolute; inset:auto -70px -150px auto; width:300px; height:300px; border:55px solid rgba(255,255,255,.045); border-radius:50%; }
    .header-inner { position:relative; z-index:1; width:min(1080px,calc(100% - 32px)); margin:auto; padding:26px 0 34px; display:flex; align-items:center; gap:18px; }
    .logo-wrap { flex:none; width:82px; height:82px; display:grid; place-items:center; background:#fff; border:4px solid var(--gold); border-radius:50%; box-shadow:0 8px 22px rgba(0,0,0,.18); }
    .logo-wrap img { width:68px; height:68px; object-fit:contain; }
    .eyebrow { margin:0 0 5px; color:var(--gold-light); font-size:12px; font-weight:800; text-transform:uppercase; }
    h1 { max-width:850px; margin:0; color:#fff; font-size:clamp(21px,4vw,34px); line-height:1.2; }
    .subtitle { margin:7px 0 0; color:#dbeee3; font-size:14px; line-height:1.55; }
    main { width:min(960px,calc(100% - 28px)); margin:-18px auto 48px; position:relative; z-index:2; }
    .search-panel { padding:22px; background:var(--surface); border:1px solid var(--line); border-top:5px solid var(--gold); border-radius:8px; box-shadow:var(--shadow); }
    .search-title { margin:0 0 6px; color:var(--green-900); font-size:18px; }
    .search-note { margin:0 0 16px; color:var(--muted); font-size:13px; line-height:1.5; }
    .search-box { position:relative; }
    .search-icon { position:absolute; left:15px; top:50%; transform:translateY(-50%); color:var(--green-700); }
    #student-search { width:100%; height:54px; padding:0 46px; border:2px solid var(--line); border-radius:7px; color:var(--ink); background:#fff; font-size:16px; font-weight:700; outline:none; transition:.2s; }
    #student-search:focus { border-color:var(--green-700); box-shadow:0 0 0 4px var(--green-100); }
    #clear-search { position:absolute; right:8px; top:8px; width:38px; height:38px; border:0; border-radius:50%; color:var(--muted); background:transparent; cursor:pointer; display:none; }
    #clear-search:hover { background:var(--green-50); color:var(--green-900); }
    .suggestions { position:absolute; z-index:10; top:60px; left:0; right:0; max-height:290px; overflow:auto; padding:6px; background:#fff; border:1px solid var(--line); border-radius:7px; box-shadow:0 16px 34px rgba(10,58,34,.16); }
    .suggestion { width:100%; padding:11px 12px; display:flex; align-items:center; gap:11px; border:0; border-bottom:1px solid var(--line); background:#fff; color:var(--ink); text-align:left; cursor:pointer; }
    .suggestion:last-child { border-bottom:0; }
    .suggestion:hover,.suggestion:focus { background:var(--green-50); outline:none; }
    .avatar { width:34px; height:34px; flex:none; display:grid; place-items:center; border-radius:50%; color:var(--green-800); background:var(--green-100); }
    .suggestion b { display:block; font-size:13px; }
    .suggestion small { display:block; margin-top:3px; color:var(--muted); font-size:11px; }
    .hidden { display:none !important; }
    .state { margin-top:18px; padding:36px 18px; text-align:center; color:var(--muted); background:rgba(255,255,255,.75); border:1px dashed #b9cbbf; border-radius:8px; }
    .state i { display:block; margin-bottom:12px; color:var(--green-700); font-size:32px; }
    .spinner { width:38px; height:38px; margin:0 auto 12px; border:4px solid var(--green-100); border-top-color:var(--green-700); border-radius:50%; animation:spin .8s linear infinite; }
    @keyframes spin { to { transform:rotate(360deg); } }
    .result { margin-top:18px; animation:reveal .25s ease-out; }
    @keyframes reveal { from { opacity:0; transform:translateY(7px); } }
    .identity { padding:20px 22px; display:flex; align-items:flex-start; justify-content:space-between; gap:18px; color:#fff; background:var(--green-800); border-radius:8px 8px 0 0; }
    .identity-label { margin:0 0 6px; color:var(--gold-light); font-size:11px; font-weight:800; text-transform:uppercase; }
    .identity h2 { margin:0; font-size:22px; line-height:1.3; }
    .identity-meta { margin:7px 0 0; color:#e4f2e9; font-size:13px; }
    .status { flex:none; padding:8px 11px; border-radius:5px; font-size:11px; font-weight:900; text-transform:uppercase; }
    .status.lunas { color:#075b35; background:#d8f7e6; }
    .status.menunggak { color:#8c1d15; background:#ffe1dc; }
    .summary { display:grid; grid-template-columns:repeat(3,1fr); background:#fff; border:1px solid var(--line); border-top:0; }
    .summary-item { min-width:0; padding:19px 20px; border-right:1px solid var(--line); }
    .summary-item:last-child { border-right:0; }
    .summary-label { margin:0 0 7px; color:var(--muted); font-size:11px; font-weight:800; text-transform:uppercase; }
    .summary-value { margin:0; overflow-wrap:anywhere; color:var(--green-900); font-size:clamp(16px,3vw,23px); font-weight:900; }
    .summary-value.danger { color:var(--danger); }
    .student-info { display:grid; grid-template-columns:repeat(4,1fr); padding:15px 20px; gap:12px; background:#fbfdfb; border:1px solid var(--line); border-top:0; }
    .info-label { display:block; margin-bottom:3px; color:var(--muted); font-size:10px; text-transform:uppercase; font-weight:800; }
    .info-value { color:var(--ink); font-size:13px; font-weight:700; }
    .history { margin-top:16px; background:#fff; border:1px solid var(--line); border-radius:8px; overflow:hidden; box-shadow:0 9px 24px rgba(10,58,34,.06); }
    .section-heading { padding:16px 18px; display:flex; align-items:center; justify-content:space-between; gap:12px; border-bottom:1px solid var(--line); }
    .section-heading h3 { margin:0; color:var(--green-900); font-size:16px; }
    .count { padding:5px 8px; color:var(--green-800); background:var(--green-100); border-radius:4px; font-size:11px; font-weight:800; }
    .table-wrap { overflow-x:auto; }
    table { width:100%; min-width:760px; border-collapse:collapse; }
    th { padding:11px 12px; color:#fff; background:var(--green-800); font-size:10px; text-align:left; text-transform:uppercase; }
    td { padding:12px; border-bottom:1px solid var(--line); color:#34443a; font-size:12px; vertical-align:top; }
    tbody tr:nth-child(even) { background:#f8fbf9; }
    td.num,th.num { text-align:right; white-space:nowrap; }
    td.center,th.center { text-align:center; }
    .empty-row { padding:26px; text-align:center; color:var(--muted); }
    tfoot td { color:var(--green-900); background:var(--gold-light); font-weight:900; }
    .notice { margin-top:16px; padding:18px; display:flex; align-items:center; justify-content:space-between; gap:18px; background:#fff9e6; border:1px solid #ecd584; border-left:5px solid var(--gold); border-radius:8px; }
    .notice-copy { display:flex; gap:13px; align-items:flex-start; }
    .notice-icon { width:38px; height:38px; flex:none; display:grid; place-items:center; color:#775a09; background:var(--gold-light); border-radius:50%; }
    .notice h3 { margin:0 0 5px; color:#5c470b; font-size:14px; }
    .notice p { margin:0; color:#715f2c; font-size:12px; line-height:1.55; }
    .wa-button { flex:none; min-height:44px; padding:0 15px; display:inline-flex; align-items:center; justify-content:center; gap:8px; color:#fff; background:#168b50; border-radius:6px; text-decoration:none; font-size:12px; font-weight:900; transition:.2s; }
    .wa-button:hover { background:#0e7140; transform:translateY(-1px); }
    footer { padding:0 20px 24px; color:#65756b; text-align:center; font-size:11px; line-height:1.55; }
    @media(max-width:680px) {
      .header-inner { width:calc(100% - 24px); padding:20px 0 30px; align-items:flex-start; gap:12px; }
      .logo-wrap { width:62px; height:62px; border-width:3px; }
      .logo-wrap img { width:51px; height:51px; }
      .subtitle { font-size:12px; }
      main { width:calc(100% - 20px); }
      .search-panel { padding:17px 14px; }
      .summary { grid-template-columns:1fr; }
      .summary-item { padding:15px 17px; border-right:0; border-bottom:1px solid var(--line); }
      .summary-item:last-child { border-bottom:0; }
      .student-info { grid-template-columns:1fr 1fr; padding:14px 16px; }
      .identity { padding:17px; flex-direction:column; }
      .notice { align-items:stretch; flex-direction:column; }
      .wa-button { width:100%; }
    }
    @media(prefers-reduced-motion:reduce) { *,*::before,*::after { animation:none !important; transition:none !important; } }
  </style>
</head>
<body>
  <div class="topbar"></div>
  <header class="site-header">
    <div class="header-inner">
      <div class="logo-wrap"><img src="https://i.ibb.co.com/W49zb9n3/logo-yayasan-Terbaru.png" alt="Logo Yayasan Huda Wan Nurul Qurani"></div>
      <div>
        <p class="eyebrow">Layanan Informasi Orang Tua/Wali</p>
        <h1>Laporan Infaq Santri</h1>
        <p class="subtitle">Yayasan Huda Wan Nurul Qurani<br>TK–SD–SMP International Tahfizh Quran</p>
      </div>
    </div>
  </header>

  <main>
    <section class="search-panel" aria-labelledby="search-title">
      <h2 class="search-title" id="search-title">Cari Nama Santri</h2>
      <p class="search-note">Ketik minimal 1 huruf, lalu pilih nama santri untuk melihat rincian pembayaran.</p>
      <div class="search-box">
        <i class="fa-solid fa-magnifying-glass search-icon" aria-hidden="true"></i>
        <input id="student-search" type="search" autocomplete="off" placeholder="Ketik nama santri…" aria-label="Cari nama santri" aria-controls="suggestions">
        <button id="clear-search" type="button" aria-label="Hapus pencarian"><i class="fa-solid fa-xmark"></i></button>
        <div id="suggestions" class="suggestions hidden" role="listbox"></div>
      </div>
    </section>

    <section id="loading-state" class="state"><div class="spinner"></div><strong>Memuat data pembayaran…</strong><br><small>Mohon tunggu sebentar.</small></section>
    <section id="empty-state" class="state hidden"><i class="fa-solid fa-user-check"></i><strong>Silakan cari nama santri</strong><br><small>Rincian hanya ditampilkan setelah nama dipilih.</small></section>
    <section id="not-found-state" class="state hidden"><i class="fa-solid fa-magnifying-glass"></i><strong>Nama santri tidak ditemukan</strong><br><small>Periksa kembali ejaan nama yang diketik.</small></section>
    <section id="error-state" class="state hidden"><i class="fa-solid fa-triangle-exclamation"></i><strong>Data belum dapat dimuat</strong><br><small>Periksa koneksi internet, kemudian muat ulang halaman.</small></section>

    <section id="student-result" class="result hidden" aria-live="polite">
      <div class="identity">
        <div><p class="identity-label">Rincian Santri</p><h2 id="student-name">—</h2><p id="student-meta" class="identity-meta">—</p></div>
        <span id="payment-status" class="status">—</span>
      </div>
      <div class="summary">
        <div class="summary-item"><p class="summary-label">Total Pembayaran Tercatat</p><p id="total-paid" class="summary-value">Rp0</p></div>
        <div class="summary-item"><p class="summary-label">Total Tunggakan</p><p id="total-due" class="summary-value danger">Rp0</p></div>
        <div class="summary-item"><p class="summary-label">Infaq Bulanan</p><p id="monthly-fee" class="summary-value">Rp0</p></div>
      </div>
      <div class="student-info">
        <div><span class="info-label">Jenjang</span><span id="student-level" class="info-value">—</span></div>
        <div><span class="info-label">Kelas</span><span id="student-class" class="info-value">—</span></div>
        <div><span class="info-label">Jalur</span><span id="student-track" class="info-value">—</span></div>
        <div><span class="info-label">Bulan Tunggakan</span><span id="due-months" class="info-value">—</span></div>
      </div>

      <div class="history">
        <div class="section-heading"><h3><i class="fa-solid fa-receipt"></i> Riwayat Pembayaran Lengkap</h3><span id="history-count" class="count">0 transaksi</span></div>
        <div class="table-wrap">
          <table>
            <thead><tr><th class="center">No</th><th>Tanggal</th><th>Bulan</th><th>Keterangan Bayar</th><th>Rincian</th><th>Dibayar Kepada</th><th class="num">Pembayaran</th></tr></thead>
            <tbody id="history-body"></tbody>
            <tfoot><tr><td colspan="6">TOTAL PEMBAYARAN TERCATAT</td><td id="history-total" class="num">Rp0</td></tr></tfoot>
          </table>
        </div>
      </div>
    </section>

    <aside class="notice">
      <div class="notice-copy">
        <div class="notice-icon"><i class="fa-solid fa-circle-info"></i></div>
        <div><h3>Sudah membayar tetapi data belum masuk?</h3><p>Mohon segera lapor dan kirimkan bukti pembayaran kepada Bendahara Yayasan, <strong>Kamelianda, SE</strong>.</p></div>
      </div>
      <a id="wa-button" class="wa-button" href="https://wa.me/6285270884838" target="_blank" rel="noopener"><i class="fa-brands fa-whatsapp"></i> Hubungi Bendahara</a>
    </aside>
  </main>

  <footer>Data ditampilkan sesuai catatan pembayaran Yayasan Huda Wan Nurul Qurani.</footer>

  <script>
    const URL_TUNGGAKAN = "https://docs.google.com/spreadsheets/d/e/2PACX-1vRR3UGAaAu88VLk17PoUu_6h3bUmT3O4usNKDqxHGSGly8tCQYjM6gQCe1GXFD6jc3lOTvvhOH0ilto/pub?gid=917422507&single=true&output=csv";
    const URL_PEMBAYARAN = "https://docs.google.com/spreadsheets/d/e/2PACX-1vRR3UGAaAu88VLk17PoUu_6h3bUmT3O4usNKDqxHGSGly8tCQYjM6gQCe1GXFD6jc3lOTvvhOH0ilto/pub?gid=280283648&single=true&output=csv";
    const BENDAHARA_WA = "6285270884838";
    let dataTunggakan = [], dataPembayaran = [], studentNames = [], selectedName = "";

    const $ = id => document.getElementById(id);
    const normalizeName = value => String(value || "").trim().replace(/\s+/g, " ");
    const escapeHTML = value => String(value ?? "").replace(/[&<>'"]/g, c => ({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]));
    function getColValue(row, names) {
      const keys = Object.keys(row || {});
      for (const name of names) {
        const key = keys.find(k => k.trim().toLowerCase() === name.toLowerCase());
        if (key && row[key] !== undefined && row[key] !== "") return row[key];
      }
      for (const name of names) {
        const key = keys.find(k => name.length > 2 && k.trim().toLowerCase().includes(name.toLowerCase()));
        if (key && row[key] !== undefined && row[key] !== "") return row[key];
      }
      return "";
    }
    const getName = row => normalizeName(getColValue(row,["NAMA SANTRI 2026","Nama Santri","Nama Siswa","Nama"]));
    function parseNumeric(value) { return parseInt(String(value || "").replace(/[^\d-]/g,""),10) || 0; }
    function rupiah(value) { return new Intl.NumberFormat("id-ID",{style:"currency",currency:"IDR",minimumFractionDigits:0}).format(value || 0); }
    function setOnlyState(id) {
      ["loading-state","empty-state","not-found-state","error-state","student-result"].forEach(key => $(key).classList.toggle("hidden", key !== id));
    }
    function buildStudentNames() {
      const map = new Map();
      [...dataTunggakan,...dataPembayaran].forEach(row => {
        const name = getName(row); if (!name) return;
        const key = name.toLocaleLowerCase("id-ID"); if (!map.has(key)) map.set(key,name);
      });
      studentNames = [...map.values()].sort((a,b) => a.localeCompare(b,"id-ID"));
    }
    async function loadData() {
      try {
        const [tunggakanResponse,pembayaranResponse] = await Promise.all([fetch(URL_TUNGGAKAN),fetch(URL_PEMBAYARAN)]);
        if (!tunggakanResponse.ok || !pembayaranResponse.ok) throw new Error("Sumber data tidak tersedia");
        const [tunggakanCSV,pembayaranCSV] = await Promise.all([tunggakanResponse.text(),pembayaranResponse.text()]);
        dataTunggakan = Papa.parse(tunggakanCSV,{header:true,skipEmptyLines:true}).data;
        dataPembayaran = Papa.parse(pembayaranCSV,{header:true,skipEmptyLines:true}).data;
        buildStudentNames(); setOnlyState("empty-state"); $("student-search").disabled = false; $("student-search").focus();
      } catch (error) { console.error(error); setOnlyState("error-state"); }
    }
    function showSuggestions(query) {
      const box = $("suggestions"); box.innerHTML = "";
      const clean = normalizeName(query).toLocaleLowerCase("id-ID");
      if (!clean) { box.classList.add("hidden"); setOnlyState(selectedName ? "student-result" : "empty-state"); return; }
      const matches = studentNames.filter(name => name.toLocaleLowerCase("id-ID").includes(clean)).slice(0,20);
      if (!matches.length) { box.classList.add("hidden"); setOnlyState("not-found-state"); return; }
      matches.forEach(name => {
        const row = dataTunggakan.find(r => getName(r).toLocaleLowerCase("id-ID") === name.toLocaleLowerCase("id-ID"));
        const button = document.createElement("button"); button.type="button"; button.className="suggestion"; button.setAttribute("role","option");
        button.innerHTML = `<span class="avatar"><i class="fa-solid fa-user-graduate"></i></span><span><b>${escapeHTML(name)}</b><small>${escapeHTML(getColValue(row || {},["Jenjang"]) || "Santri")} · Kelas ${escapeHTML(getColValue(row || {},["Kelas"]) || "—")}</small></span>`;
        button.addEventListener("click",() => selectStudent(name)); box.appendChild(button);
      });
      box.classList.remove("hidden");
    }
    function selectStudent(name) {
      selectedName = name; $("student-search").value = name; $("suggestions").classList.add("hidden"); $("clear-search").style.display="block";
      const key = name.toLocaleLowerCase("id-ID");
      const account = dataTunggakan.find(r => getName(r).toLocaleLowerCase("id-ID") === key) || {};
      const payments = dataPembayaran.filter(r => getName(r).toLocaleLowerCase("id-ID") === key);
      const totalPaid = payments.reduce((sum,row) => sum + parseNumeric(getColValue(row,["PEMBAYARAN"])),0);
      const totalDue = parseNumeric(getColValue(account,["TOTAL TUNGGAKAN","Tunggakan"]));
      $("student-name").textContent = name.toUpperCase();
      $("student-meta").textContent = `${getColValue(account,["Jenjang"]) || getColValue(payments[0] || {},["Jenjang"]) || "Santri"} · Data pembayaran tercatat yayasan`;
      $("total-paid").textContent = rupiah(totalPaid); $("total-due").textContent = rupiah(totalDue);
      $("monthly-fee").textContent = rupiah(parseNumeric(getColValue(account,["Bayar Bulanan","BAYAR BULANAN"])));
      $("student-level").textContent = getColValue(account,["Jenjang"]) || getColValue(payments[0] || {},["Jenjang"]) || "—";
      $("student-class").textContent = getColValue(account,["Kelas"]) || "—";
      $("student-track").textContent = getColValue(account,["Masuk Jalur","Jalur Masuk"]) || "—";
      $("due-months").textContent = getColValue(account,["Bulan"]) || (totalDue > 0 ? "Ada tunggakan" : "Tidak ada");
      const status = $("payment-status"); status.textContent = totalDue > 0 ? "Ada Tunggakan" : "Tidak Ada Tunggakan"; status.className = `status ${totalDue > 0 ? "menunggak" : "lunas"}`;
      const body = $("history-body"); body.innerHTML = "";
      payments.forEach((row,index) => {
        const tr = document.createElement("tr");
        tr.innerHTML = `<td class="center">${index+1}</td><td>${escapeHTML(getColValue(row,["TANGGAL","Tanggal","Tgl"]) || "—")}</td><td>${escapeHTML(getColValue(row,["BULAN","Bulan"]) || "—")}</td><td>${escapeHTML(getColValue(row,["KETERANGAN BAYAR"]) || "—")}</td><td>${escapeHTML(getColValue(row,["KETERANGAN","Ket"]) || "—")}</td><td>${escapeHTML(getColValue(row,["DIBAYAR KEPADA"]) || "—")}</td><td class="num"><strong>${rupiah(parseNumeric(getColValue(row,["PEMBAYARAN"])))}</strong></td>`;
        body.appendChild(tr);
      });
      if (!payments.length) body.innerHTML = `<tr><td colspan="7" class="empty-row">Belum ada riwayat pembayaran yang tercatat.</td></tr>`;
      $("history-count").textContent = `${payments.length} transaksi`; $("history-total").textContent = rupiah(totalPaid);
      const message = `Assalamualaikum, saya orang tua/wali santri ${name}. Saya sudah melakukan pembayaran, tetapi data belum tercatat. Mohon bantuan pengecekannya. Terima kasih.`;
      $("wa-button").href = `https://wa.me/${BENDAHARA_WA}?text=${encodeURIComponent(message)}`;
      setOnlyState("student-result");
    }
    $("student-search").disabled = true;
    $("student-search").addEventListener("input",event => { selectedName=""; $("clear-search").style.display=event.target.value ? "block" : "none"; showSuggestions(event.target.value); });
    $("student-search").addEventListener("keydown",event => { if (event.key === "Escape") $("suggestions").classList.add("hidden"); });
    $("clear-search").addEventListener("click",() => { selectedName=""; $("student-search").value=""; $("clear-search").style.display="none"; $("suggestions").classList.add("hidden"); setOnlyState("empty-state"); $("student-search").focus(); });
    document.addEventListener("click",event => { if (!event.target.closest(".search-box")) $("suggestions").classList.add("hidden"); });
    loadData();
  </script>
</body>
</html>
